This folder contains deployment files like Dockerfile and deployment scripts.
Deployment Summary
The Blockchain-Based Academic Certificate Verification System was successfully deployed as a Flask web application in a cloud hosting environment.

The application backend (Flask, Python libraries) and frontend (HTML templates) were organized and deployed.

Required dependencies included PyPDF2, Pandas, pdf2image, and Tesseract OCR.

After deployment, the application URL was accessible and all modules were tested.

Verification activities confirmed that registration, login, dashboard access, manual certificate verification, PDF upload and OCR processing, AI assistant chat, and logout all worked correctly.

All deployment tests passed, validating that the system functions reliably in the live environment.

Result: The deployment task was completed successfully, and the application responded as expected across all major features.
